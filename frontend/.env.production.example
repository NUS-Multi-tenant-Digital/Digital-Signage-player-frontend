# 生产构建用后端地址（必须是可公网访问的 HTTPS 地址）。
# 复制为 .env.production 后修改，再执行 npm run build。
#
# 该域名需与后端 APP_CORS_ALLOWED_ORIGINS 中登记的前端域名相匹配，
# 且若前端走 HTTPS，后端也必须是 HTTPS，否则浏览器会拦截混合内容。
VITE_API_BASE_URL=https://api.signage.example.com
