# 播放器后端地址（Player Application Server）。
#
# - 设置为后端地址时，前端走真实 HTTP 接口联调，例如：
#     VITE_API_BASE_URL=http://localhost:3000
# - 留空或删除本变量时，前端回退到本地 Mock，可脱离后端独立演示。
#
# 使用方式：把本文件复制为 .env 后按需修改。
VITE_API_BASE_URL=
